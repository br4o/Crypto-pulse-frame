export default function handler(req, res) {
  // Placeholder endpoint for Farcaster Frame integration.
  // Replace with real Frame SDK usage when ready.
  res.status(200).json({ ok: true, message: 'Frame endpoint placeholder' });
}
